import React from "react";
import Base from "../core/Base";

const UserDashboard = () => {
  return (
    <Base title="User dashboard" description="Dashboard for users">
      <h1>Welcome to user dashboard page</h1>
    </Base>
  );
};

export default UserDashboard;
